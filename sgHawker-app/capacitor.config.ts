import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sgHawker.app',
  appName: 'sgHawker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
